# GitHub repository metadata. Applied by the repository-settings app
# (https://github.com/apps/settings). Run `bash scripts/finalize.sh` to
# also apply description + topics via `gh repo edit`.

repository:
  name: claude-code-ssh
  description: "MCP server that gives Claude Code direct SSH access to your server fleet. 51 tools, connection pooled, per-user gated, ASCII output."
  homepage: https://github.com/hunchom/claude-code-ssh

  topics:
    - mcp
    - mcp-server
    - model-context-protocol
    - claude-code
    - claude
    - anthropic
    - ssh
    - ssh-automation
    - devops
    - sre
    - automation
    - remote-server
    - deployment
    - backup
    - monitoring
    - nodejs
